---
name: Auto-Assign Codex v2

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  assign:
    runs-on: ubuntu-latest

    steps:
      - run: |
          gh issue edit "${{ github.event.issue.number }}" \
            --add-label "codex" \
            --add-label "triage"
